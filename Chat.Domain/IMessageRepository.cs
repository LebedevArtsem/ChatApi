namespace Chat.Domain;
public interface IMessageRepository
{
    //Task<ICollection<ChatMessage>> Get();

    //Task<ChatMessage> GetById(int id);

    //Task Create(ChatMessage message);

    //Task Update(int id, ChatMessage message);

    //Task Delete(int id);
}

